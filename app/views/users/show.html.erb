 <div class="background-profil">
  <div class="profile-card">
    <div id="login-container">
        <div class="profile-img" style="background-image: url(<%= cl_image_path current_user.photo.key %>)"></div>
          <div class="nickname-age">
            <h1><%= current_user.nickname%></h1>
            <h3><%= current_user.age %> ans</h3>
          </div>

        <div class="description">
          <p><%= current_user.description %></p>
        </div>
    </div>
    <div class="bookings">
      <div class="proficard-title">
        <i class="fas fa-star"></i><h2>My games</h2><i class="fas fa-star"></i>
      </div>
        <button class="trigger">See historic</button>
          <div class="booking-field">
              <% current_user.bookings.each do |booking| %>
                <ul>
                  <div class="booked">
                    <li>
                      Reserved <%= link_to booking.offer.title, offer_path(booking.offer) %> for <%= booking.status %>
                      
                      <%= link_to booking_path(booking), method: :delete do %>
                          <i class="far fa-times-circle"></i>
                        <% end %>
                    </li>
                  </div>
                </ul>
              <% end %>
          </div>
  </div>

</div>

  <div class="modal">
  <div class="overlay"></div>
    <div class="modal-content">
      <span class="close-button">&times;</span>
        <div class="historic">
          <%# <i class="far fa-futbol"><i class="far fa-futbol"></i> %>
          <h1>Historic</h1>
        </div>
              <div class="passed-games">
                <% @old_bookings.each do |old_booking| %>
                  <ul>
                    <div class="game">
                      <li><%=old_booking.offer.title%></li>
                    </div>
                  </ul>
                <% end %>
              </div>

              <div class="palmares">
                <div class="title">
                <i class="fas fa-trophy"></i><h1>Ranking</h1><i class="fas fa-trophy"></i>
                </div>
                  <div class="palmares-text">
                    <%= games = @old_bookings.count %><%= " games" %><i class="fas fa-check"></i>
                    <br>
                    <p><% case games
                      when 1..3 %>
                        You're a District player!
                      <% when 3..7 %>
                        You're a Semi-pro!
                      <% when 7..100 %>
                        You're a Legend, the Master of the field!
                    <% end %></p>
                  </div>
                  <br>
              <div class="next-step">
                <% case games
                 when 1..3 %>
                    <i class="fas fa-arrow-right"></i> Go for more games !
                  <% when 3..7 %>
                    <i class="fas fa-arrow-right"></i> Congrats,  !
                  <% when 7..100 %>
                    <i class="fas fa-arrow-right"></i> Ultimate step, You are Awesome
                <% end %></p>
              </div>
          </div>
    </div>
  </div>
  </div>


